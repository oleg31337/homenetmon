version: '3'
services:
  homenetmon:
    container_name: homenetmon
    build: .
    restart: unless-stopped
    volumes:
      - homenetmon-data:/data
    network_mode: host
volumes:
  homenetmon-data: